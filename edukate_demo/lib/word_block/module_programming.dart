import 'package:flutter/material.dart';

class ModuleProgrammingPage extends StatelessWidget {
  const ModuleProgrammingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('word block interface'),
      ),
      body: const Center(
        child: Text('content'),
      ),
    );
  }
}
