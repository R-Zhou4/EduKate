import 'package:flutter/material.dart';

class TextProgrammingPage extends StatelessWidget {
  const TextProgrammingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('text coding interface'),
      ),
      body: const Center(
        child: Text('content'),
      ),
    );
  }
}
